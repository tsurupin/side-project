import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    container: {},
    cardListContainer: {
        width: '100%',
        flex: 1,
        flexDirection: 'row'
    }
});

export default styles;