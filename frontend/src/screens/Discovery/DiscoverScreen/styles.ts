import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  cardListContainer: {
    width: "100%",
    flex: 1,
    flexDirection: "row"
  }
});

export default styles;
