import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginBottom: 20
  },

  labelText: {
    fontSize: 16,
    fontWeight: "bold",
    color: "grey",
    marginBottom: 5
  },
  text: {
    fontSize: 16
  }
});

export default styles;
