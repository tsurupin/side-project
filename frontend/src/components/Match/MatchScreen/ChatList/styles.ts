import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    
  },
  itemContainer: {
    height: 80
  },
  itemTitle: {
    fontSize: 20,
    fontWeight: "bold"
  }
})

export default styles;