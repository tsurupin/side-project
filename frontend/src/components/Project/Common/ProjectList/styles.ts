import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 80,
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
  },
});

export default styles;
