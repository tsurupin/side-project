import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {

  },
  itemContainer: {
    height: 60
  },
  itemTitle: {
    fontSize: 16
  }
});

export default styles;