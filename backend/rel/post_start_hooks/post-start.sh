#!/bin/sh
set -e

echo "Connecting nodes"
##release_ctl eval --mfa "Elixir.Db.Tasks.BootTasks.test/0"
##release_ctl eval --mfa "Elixir.Db.Tasks.BootTasks.connect/0"
#release_ctl ping --name="$APP_NAME" --cookie="$NODE_COOKIE" "$@"
